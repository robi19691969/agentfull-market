import matplotlib.pyplot as plt\nimport pandas as pd\nimport numpy as np\n\ndef generate_investment_report(data):\n    # Creating charts\n    plt.figure(figsize=(10, 5))\n    plt.plot(data['Date'], data['Price'], label='Price Trend')\n    plt.title('Investment Price Trend')\n    plt.xlabel('Date')\n    plt.ylabel('Price')\n    plt.legend()\n    plt.savefig('price_trend.png')\n    plt.close()\n\n    # Generating text report\n    avg_price = np.mean(data['Price'])\n    recommendations = 'Invest' if avg_price < data['Price'].iloc[-1] else 'Hold'\n    report = f"Investment Report:\n    Average Price: {avg_price}\n    Recommendation: {recommendations}"\n\n    # Saving the report\n    with open('investment_report.txt', 'w') as f:\n        f.write(report)\n\nif __name__ == '__main__':\n    # Sample data generation\n    dates = pd.date_range(start='2026-01-01', periods=100)\n    prices = np.random.randn(100).cumsum() + 100  # Random walk\n    investment_data = pd.DataFrame({'Date': dates, 'Price': prices})\n\n    generate_investment_report(investment_data)\n